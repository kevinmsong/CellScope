# CellScope runtime dependencies (the same as `pip install -e ".[cellpose]"`).
# pyproject.toml is the source of truth; development tools are in the `dev` extra.
#
# numpy is pinned <2 because the scientific stack this is tested with
# (torch 2.5.1+cu124, scipy 1.11) is built against the 1.26 ABI.
#
# cellpose 4.x (Cellpose-SAM) is a breaking change from 3.x. For a GPU, install
# the CUDA build of PyTorch before this file (see README).
#
# gradio is pinned <6 for a stable Blocks / State / event API.

gradio>=5.30,<6
cellpose>=4.0,<5
scikit-image>=0.24
numpy>=1.26,<2
pandas>=2.0
scipy>=1.11
Pillow>=10
plotly>=5.20
tifffile>=2024.1
reportlab>=4.0,<5
